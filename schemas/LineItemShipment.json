{
    "type": "object",
    "properties": {
        "shipmentID": {
            "type": "string",
            "maxLength": 5000
        },
        "quantity": {
            "type": "number"
        },
        "assignedAt": {
            "description": "The date on which the shipment was assigned to the purchase order.",
            "type": "string",
            "format": "date-time",
            "maxLength": 5000
        }
    },
    "required": ["shipmentID", "assignedAt", "quantity"]
}

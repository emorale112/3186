{
    "type": "object",
    "description": "Cancel purchase order(s).",
    "properties": {
        "items": {
            "type": "array",
            "items": {
                "type": "object",
                "required": ["reference"],
                "properties": {
                    "reference": {
                        "$ref": "../../BuyerPurchaseOrderReference.json"
                    }
                }
            },
            "description": "An array of purchase orders to be cancelled."
        }
    },
    "required": ["items"]
}

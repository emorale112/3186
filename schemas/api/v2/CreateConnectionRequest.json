{
    "type": "object",
    "properties": {
        "token": {
            "type": "string",
            "description": "An activation token for the connection."
        }
    },
    "required": ["token"]
}
